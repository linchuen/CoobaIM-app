export enum ChatType {
    ToUser = "user",
    ToRoom = "room",
    ToAll = "all",
    ToNobody = "",
}

export enum RoleType {
    AGENT = "AGENT",
    ADMIN = "ADMIN",
    GUEST = "GUEST",
    USER = "USER",
}

export enum TabType {
    GROUP = "group",
    PERSONAL = "personal",
    FRIEND_APPLY = "friendApply",
}
